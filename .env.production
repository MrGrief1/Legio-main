# Production Environment Variables for Railway

# Backend URL (will be proxied через nginx в production, or direct URL for API)
# For Railway MONOREPO: Leave EMPTY to use relative URLs (e.g., /api/...)
# For Railway SEPARATE SERVICES: Set to https://your-backend.railway.app
VITE_API_BASE_URL=

# API endpoint path (usually /api)
VITE_API_URL=/api
